# 러닝봄 vNext 2차 인계

작성 시점: 2026-07-26. 기준선 `44a15fd` → 2차 결과 `e4f7626` 계열.

이 문서의 주장을 그대로 믿지 말고 최신 `main`, Actions 실행 결과, 실제 기기에서 다시 확인하십시오.

## Play 안전 선언

```text
PLAY INTERNAL CHANGED: NO
PLAY CLOSED CHANGED: NO
PLAY PRODUCTION CHANGED: NO
EXISTING CLOSED TESTERS IMPACTED: NO
```

- `.github/workflows/play-release.yml`은 이번 작업에서 열어보기만 했고 수정하지 않았습니다.
- 정식 패키지 `kr.robom.runningbom`의 versionCode·서명·app 설정은 변경하지 않았습니다.
- 이번에 추가된 배포 경로는 `preview` 프로필 APK 하나뿐이며 `eas submit`이나 Play 업로드는 포함하지 않습니다.

## 무엇이 바뀌었나

| 영역 | 이전 | 이번 |
| --- | --- | --- |
| 화면 이동 | 하단 5탭 | 좌상단 햄버거 드로어, 캘린더·대회·러닝화·기록통계 독립 메뉴 |
| 러닝화 | 9종 | 123종, 3대 카테고리 9세부, 다축 필터, 추천 마법사, 비교함 |
| 코치 발화 | 110~300초 간격 | 간단 26초 / 보통 15초 / 자세히 9초 |
| 멘트 풀 | 약 60문장 | 743문장(공용 318 + 구간 234 + 유형 전용 191) |
| 러닝 유형 | 5종, 기준 모호 | 15종, 강도·목적·대상·구성 명시 |
| 음성 | 기기 기본 | 남성/여성 선택, 품질 높은 한국어 음성 자동 선택, 미리듣기 |
| 대회 집계 | 종목별 행이 각각 1건 | 대회 단위 1건(`raceGroupKey`), 달력 뷰·목표 대회·준비 체크리스트 |
| 배지 | 소수 | 44종 9카테고리, 진행률 표시 |
| 주간 기준 | 주 3회 고정 | 최근 4주 기반 자동 추천(횟수·시간·거리 중 선택) |
| 커뮤니티 | 읽기 전용 피드 | Q&A 지식 카드 40개 6카테고리 + 로컬 임시 보관함 |
| Preview 배포 | 수동 로컬 빌드 | Actions 자동 빌드 + GitHub Release + 인앱 업데이트 안내 |

## 정직성 경계 (지키고 있는 것)

- 러닝화의 무게·드롭·스택·원화 가격은 **필드 자체를 만들지 않았습니다.** 가격은 `priceBand`만 씁니다.
- 공식 기술명(`keyTech`)은 123종 중 36종만 채웠고 나머지는 비어 있습니다. 모르는 것은 비웁니다.
- 국내 공식 구매 링크는 확인된 브랜드만 노출하고, 나머지는 버튼 자체를 렌더하지 않습니다. 해외몰은 연결하지 않습니다.
- "최저가"라는 단정 표현은 금지이며 테스트로 차단합니다.
- Samsung Health·Garmin·Nike는 "준비 중"으로만 표시합니다.
- 소셜 로그인 비활성 사유를 설정 화면에 사용자 언어로 적었습니다.
- 통증 관련 Q&A는 전문가 상담 안내를 포함해야 하며 테스트로 강제합니다.

## 검증

```bash
cd apps/mobile && npm run check
# tests 186 / pass 186 / fail 0, typecheck 오류 0
```

- 러닝화 테스트 47개, 코칭 테스트 다수, 커뮤니티·대회·통계 신규 45개를 포함합니다.
- 러닝화 주간 점검: `node --import ./apps/mobile/node_modules/tsx/dist/loader.mjs scripts/refresh-shoe-data.mjs` → 무결성 0건.

## 아직 하지 않은 것 / 다음 사람 몫

1. **실기기 장시간 코칭 검증**(20·40·60분, 화면 잠금·절전·전화·이어폰 해제)은 하지 않았습니다. 이 환경에는 Android 기기가 없습니다.
2. **러닝화 공식 확인**: 115종이 차트 기반 분류 상태입니다. `data/shoe-verification-queue.json` 우선순위대로 국내 공식 페이지를 확인해 주십시오.
3. **버전명 상향**: `apps/mobile/package.json`의 `0.19.0`을 올리려면 본사 `robom` 저장소의 패밀리 정본(`generated/robom-family/app-meta.json`, `family.lock.json`)을 함께 재생성해야 합니다. 그래서 이번에는 versionCode만 올렸습니다. 자세한 사유는 `PREVIEW_APK.md` 9절에 있습니다.
4. **커뮤니티 서버 쓰기**와 소셜 로그인은 운영 자격증명이 연결된 뒤의 작업입니다.

## 롤백

- 소스: `git revert`로 이번 커밋 범위를 되돌립니다. 기준선은 `44a15fd`입니다.
- Preview 설치본: 이전 릴리스 태그의 APK를 다시 설치하면 됩니다.
- Play 트랙은 이번 작업의 영향을 받지 않으므로 롤백 대상이 아닙니다.
