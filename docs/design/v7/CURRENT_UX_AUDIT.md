# V7 — 지금 왜 복잡한가 (코드 증거)

측정 기준: `28851a4` (2026-07-28) · 전부 실제 코드에서 센 값입니다.

## P0 — 전역 내비게이션이 두 개

| | 개수 | 파일 |
|---|---:|---|
| 좌상단 햄버거 | 모든 화면 | `app/navigation/AppHeader.tsx` |
| 드로어 상위 항목 | **13** | `app/navigation/routes.ts` |
| 드로어 하위 항목 | **14** | `app/navigation/routes.ts` |
| 하단 탭 | **5** | `domains/navigation/tabs.ts` |
| 라우트 | **16** | `app/navigation/types.ts` |

같은 목적지가 두 곳에 있습니다. 설정은 드로어에도 있고 탭(나)에도 있고,
러닝화·대회도 마찬가지입니다. 어느 쪽이 그 기능의 "진짜 자리"인지 알 수 없습니다.

모든 화면에 햄버거가 있어서 **지금 보고 있는 것이 최상위인지 그 안으로 들어온 화면인지**
구분되지 않습니다. 뒤로 가고 싶은데 뒤로가기가 없고 대신 메뉴가 열립니다.

## P0 — 한 파일이 화면 네 개를 겸함

| 화면 | 줄 수 |
|---|---:|
| `StartScreen.tsx` | **1571** |
| `RaceScreen.tsx` | 931 |
| `MyScreen.tsx` | 797 |
| `HomeScreen.tsx` | 582 |
| `SettingsScreen.tsx` | 594 |
| `ProgramsScreen.tsx` | 487 |

`StartScreen` 하나에 **준비·실행·진행·완료**가 전부 들어 있고,
컨트롤이 **25개** 있습니다(칩·슬라이더·입력·버튼).

## P1 — 오늘 행동을 말하는 카드가 두 개

`HomeScreen`의 모듈 **9개**: hero, TodayCard, 주간 목표, 목표 대회, 최근 기록,
월간 지도, 러닝화 순위, Q&A, 저장 오류 배너.

hero와 TodayCard가 **둘 다 "오늘 뭘 할지"**를 말합니다.
그리고 앱의 거의 모든 기능이 홈에 진열돼 있습니다.

## P1 — 설정이 기능 설명서

`SettingsScreen`: SectionHeader **7개**, 카드 6개, 컨트롤 **22개**가 한 스크롤에 있습니다.
같은 화면에 이런 것이 섞여 있습니다.

- 자주 바꾸는 값 (목소리, 말하기 속도, 코치 성격)
- 거의 안 바꾸는 값 (자동 멈춤 내부 임계값, 몸무게)
- **연결되지 않은 것들** (비활성 로그인 공급자, 미연동 외부 서비스)
- **운영자 진단정보** (versionCode, SHA, 데이터 revision, DB schema)

## P1 — 프로필 안에 러닝화 카탈로그

`ProfileScreen`(370줄)에서 러닝화 관련 참조가 **33곳**입니다.
프로필은 닉네임·소개·경력을 고치는 곳인데 **123종 신발 검색**이 그 안에 있습니다.

## P1 — 대회 목록 앞의 필터 벽

`RaceScreen`에서 목록보다 먼저 보이는 것이 **10종**입니다.
검색·새로고침·지역·빠른 필터·정렬·거리·접수 상태·시기·목록·달력.

`새로고침`이 검색 옆 영구 버튼입니다.

## P1 — 준비 안 된 기능이 전역 메뉴에

`CommunityScreen`에 "준비 중" 문구가 **3곳**입니다 — 사람들 소식·크루·리그.
서버가 없는 기능이 최상위 목적지로 노출됩니다.

## 보존해야 할 것 (전부 살립니다)

| | 개수 |
|---|---:|
| 훈련 계획 | 40 |
| 일회성 훈련 | 103 |
| 도전 | 40 |
| 보조 프로젝트 | 20 |
| 러닝화 | 123 |
| 배지 | 48 |
| 러닝 유형 | 15 |
| 코치 성격 | 7 |
| 긴 이야기 | 46덩어리 254문장 |
| 저장 키 | 23 |

## 기준선

- 테스트 1148 pass / 0 fail (앱) · 79 pass / 0 fail (루트)
- Production 권한 `POST_NOTIFICATIONS` 1개
- `runtimeVersion` `9bc17c3cb0a8`
- rollback SHA `28851a4`
